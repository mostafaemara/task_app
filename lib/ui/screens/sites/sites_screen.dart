import 'package:flutter/material.dart';

class SitesScreen extends StatelessWidget {
  const SitesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("sites")),
    );
  }
}
