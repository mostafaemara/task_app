import 'package:flutter/material.dart';

class AppColors {
  static const buttonsBakgroundColor = Color(0xff003455);
  static const hintTextColor = Color(0xffFAF9F7);
  static const buttonTextColor = Color(0xffFFFFFF);
  static const elevatedButtonBorderColor = Color(0xffffffff);
  static const inputBackgroundColor = Color(0x20ffffff);
  static const cityTitleColor = Color(0xff707070);
  static const contentFieldTitleColor = Color.fromRGBO(1, 2, 38, 1);
  static const contentFieldSubtitleColor = Colors.black;
  static const logOutButtonColor = Color(0xffD12A2A);
  static const colorScheme = ColorScheme(
      primary: Color(0xff003455),
      primaryVariant: Color(0xff003455),
      secondary: Color(0xff003455),
      secondaryVariant: Color(0xff003455),
      surface: Colors.white,
      background: Colors.white,
      error: Colors.red,
      onPrimary: Colors.white,
      onSecondary: Colors.white,
      onSurface: Colors.white,
      onBackground: Colors.black,
      onError: Colors.white,
      brightness: Brightness.light);
}
